# 敏感/运行态数据 —— JSON 在仓库根 json/
# 清单 json/userdata-manifest.json 由导出时扫源码注释自动生成，不入库
json/sessions.json
json/download_task.json
json/search_cache.json
json/search_task.json
server/config.json
server/*.log
test/logs/
server/sessions.json
server/download_task.json
server/search_cache.json
server/search_task.json
/userdata-manifest.json
# hash 反查：索引改为按游戏分文件存 json/index/（含本地路径、运行态数据，整体忽略不入库）
#   旧全局 3 文件（gb/local/html-name）首次启动自动迁移拆分后删除，不再使用
json/index/
json/gb-hash-index.json
json/local-hash-index.json
json/html-name-index.json
node_modules/
.DS_Store

# 真实游戏配置（含本地下载路径，敏感）——不上传；仓库只保留 gamebanana.com.json.example 示例
json/gamebanana.com.json

# 本机测试下载目录（不入库）
_test-download/
# 构建产物（release 由用户自行分发）
dist/

# 群晖 @eaDir 元数据
@eaDir/
**/@eaDir/
*.@SynoEAStream

# 构建产物（release 由用户自行分发）
dist/
release/

# 备份/日志残留（2026-09-03：统配 *.bak*，任意层级 .bak 开头备份均忽略）
*.bak*

# 本地 GitHub 推送凭据（仅本机，绝不上库）——由 scripts/git-push.sh 读取
.git-push-token
.git-credentials-local

# 运行态 PID（项目根/项目全称.pid，不入库）
*.pid

# 所有 gbmd-* 开发/需求/重构文档一律不入库（公开库），本地 docs/ 留底，权威副本在 ai-work-archive/开发者文档/
docs/gbmd-*.md

# 零依赖项目，不需要 package.json（用户明确要求）
package.json
package-lock.json
# npm 下载产物（dsh-git-push 自动追加，2026-08-20）
yarn.lock
pnpm-lock.yaml
bun.lock
npm-debug.log*
.npm/
.pnpm-store/
# 注：2026-09-02 已移除 dsh-git-push 旧版误加的 14 条「敏感字段文件」忽略项——
# 这些是项目源码/模板/油猴脚本（仅含字段名/API 参数/界面文案，无真实凭据值），
# 已由 dsh-git-push v1.13+ 根因修复（只认字符串字面量）不再误报，恢复入库跟踪。

# 旧项目历史分析报告（不入库）
分析报告/
node_modules.orig/

# 用户数据备份 zip（不入库）
*.zip

# 安全删除回收站
.trash/
**/.trash/
**/.trash-*/
json/userdata-manifest.json

# 模板素材树（由 dl-server-template 的 sync-to-project.sh 落到项目侧）
# —— 同步后项目里会有这些素材，它们是「组装来源」而非本项目源码，不入库：
#   server/templates/_<风格>-style/  风格素材（setup.sh 从这里取件组装产出）
#   server/project/blueprint/        组装蓝图（共用框架/分片/静态资源）
# 判据：它们在模板仓库维护；入库会与模板副本双份并存，模板更新后反而过期。
server/templates/
server/project/

# 模板下发的同步/组装工具（每次同步覆盖，同样在模板仓库维护）
setup.sh
assemble-manifest.js
lib-node.sh

# 框架下发件（由模板 server/framework/ 同步，同上不入库）
server/framework/
server/boot.cjs
server/lib/cjs-bootstrap.cjs
